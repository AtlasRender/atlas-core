version: "3.3"
services:
  core:
    image: pathfinder-core
    # container_name: core
    build: .
    restart: always
    ports:
      - "3002:3002"
    volumes:
      - .:/app
    expose:
      - "3002"
